package global

var (
	// RegionName stores the region name for current module
	RegionName string

	// ClusterName stores the cluster name for current module
	ClusterName string

	// ModuleName stores the module name for current module
	ModuleName string

	// ModuleName stores the host name for current module
	HostName string
)
